export default function HomePage() {
  return (
    <section className="space-y-4">
      <h2 className="text-xl font-semibold text-slate-900">Workspace Ready</h2>
      <p className="max-w-2xl text-sm leading-relaxed text-slate-700">
        The frontend is now set up with React, TypeScript, and Tailwind. Use the
        Tasks page to create and inspect task records through gRPC-web.
      </p>
    </section>
  );
}
